import 'package:flutter/material.dart';

class StateProviderWidget extends StatelessWidget {
  const StateProviderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}