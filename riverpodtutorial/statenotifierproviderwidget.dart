import 'package:flutter/material.dart';

class StateNotifierWidget extends StatelessWidget {
  const StateNotifierWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}